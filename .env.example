ANTHROPIC_API_KEY=your-api-key-here
PORT=3000

# Optional: enable HTTP Basic Auth on /chat and /session.
# If both are set, requests must include Authorization: Basic base64(user:pass).
# If unset, the server is open (use only for local dev).
BASIC_AUTH_USER=
BASIC_AUTH_PASS=
